import { describe, it, assert } from 'vitest'
import { ultraAdd } from './index'


describe('Placeholder test', () => {
  it('should have a real test', () => {
    assert.strictEqual(ultraAdd(1, 2), 3);
  });
});

